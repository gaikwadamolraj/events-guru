{
  "type": "postgres",
  "host": "localhost",
  "port": 30830,
  "username": "postgres",
  "password": "postgres",
  "database": "micro_auth",
  "synchronize": true,
  "logging": false,
  "entities": ["dist/**/*.entity.js"]
}
